import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterpush',
  appName: 'pushNotification',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
